import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  // 注意：如果你发布的仓库名不是 ai-survival-guide，请修改这里的 base 为你的仓库名
  // 例如你的仓库叫 my-guide，这里就改成 '/my-guide/'
  base: '/ai-survival-guide/', 
})
